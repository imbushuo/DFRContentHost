<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="2170" d:DesignHeight="60"
        x:Class="DFRContentHost.MainWindow"
        Title="Touch Bar Content Host" Background="Black" WindowState="Normal" CanResize="False">
  <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="10">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="120" />
      <ColumnDefinition />
      <ColumnDefinition Width="600" />
    </Grid.ColumnDefinitions>
    <!-- Esc Key -->
    <Grid Grid.Column="0">
      <Button FontSize="18" Width="100">Esc</Button>
    </Grid>
  </Grid>
</Window>
